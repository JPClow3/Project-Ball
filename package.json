{
  "name": "jonakinho",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.19.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "dev": "pnpm --filter @jonakinho/web dev",
    "format": "pnpm -r format",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter @jonakinho/web test:e2e"
  },
  "devDependencies": {
    "@changesets/cli": "2.29.7",
    "typescript": "6.0.3"
  },
  "pnpm": {
    "overrides": {
      "yaml": "2.8.3"
    }
  }
}
