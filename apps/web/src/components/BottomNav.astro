---
const items = [
  { href: "/", label: "Jogos", icon: "fa-solid fa-futbol" },
  { href: "/meus-palpites", label: "Meus", icon: "fa-solid fa-ticket" },
  { href: "/about", label: "Sobre", icon: "fa-solid fa-circle-question" },
  { href: "/support", label: "Ajuda", icon: "fa-solid fa-headset" }
] as const;
const current = Astro.url.pathname;
---

<nav class="mobile-nav" aria-label="Navegação inferior" transition:name="bottom-nav" transition:persist>
  <div class="mobile-nav-grid">
    {
      items.map((item) => {
        return (
          <a
            href={item.href}
            class="mobile-nav-link focus-ring"
            aria-current={current === item.href ? "page" : undefined}
            data-nav-link
          >
            <i class={`${item.icon} ui-icon`} style="font-size: 18px;" aria-hidden="true"></i>
            {item.label}
          </a>
        );
      })
    }
  </div>
</nav>
