{
  "name": "@project-ball/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "forge build || echo 'forge not found, skipping contracts build'",
    "check": "forge build || echo 'forge not found, skipping contracts check'",
    "format": "forge fmt --check || echo 'forge not found, skipping contracts format check'",
    "lint": "forge fmt --check || echo 'forge not found, skipping contracts lint'",
    "test": "forge test || echo 'forge not found, skipping contracts test'"
  }
}
